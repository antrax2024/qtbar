# HyprBar Configuration File

window:
  anchor: top # top, left, right or bottom
  margin_top: 0
  margin_bottom: 0
  width: 2560 # The width of the window in pixels
  height: 32 # The height of the window in pixels

  left_container:
    components:
      - type: label
        text: "Label Convencional"
        css_id: "labelLeft"

  center_container:
    components:
      - type: label
        text: "clock"
        css_id: "labelCenter"

  right_container:
    components:
      - type: label
        text: "label 1"
        css_id: "label1"
      - type: label
        text: "label2"
        css_id: "label2"
